---
label: Page
hide_body: true
fields:
- name: layout
  type: text
  config:
    required: true
  label: layout
  hidden: true
  default: page
- name: title
  type: text
  config:
    required: false
  label: Title
- name: modules
  type: blocks
  label: Modules
  template_types:
  - hero
  - content
  - banner
  - split-card
  - image-links
  - products
  - testimonials
  - article-grid
  config:
    min: 1
    max: 
- name: description
  type: textarea
  default: ''
  config:
    required: false
    wysiwyg: false
    schema:
      format: markdown
  label: Description
  description: Description for SEO purposes
- name: buybutton
  type: boolean
  label: Enable Buy Buttons
  description: If you enable this, it will be possible to create links - for instance
    buttons - that add products to the cart. The link URL format is <code>#buy=variant-id</code>,
    where <code>variant-id</code> is the Shopify Storefront ID of the variant. The
    variant with that ID specified in the URL will be placed in the cart when clicking
    the link or button.
