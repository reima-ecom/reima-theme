---
label: 'DEPRECATED: Page (old design, do not use)'
hide_body: true
fields:
- type: text
  name: layout
  label: layout
  hidden: true
  default: deprecated-page
- type: text
  name: title
  label: title
- type: textarea
  name: description
  label: description
- type: field_group_list
  name: hero
  fields:
  - type: file
    name: image
    label: Image
  - type: text
    name: heading
    label: Heading
  - type: textarea
    name: subheading
    label: Subheading
  - type: text
    name: text
    label: Text
  - type: field_group
    name: button
    label: Button
    fields:
    - type: text
      name: text
      label: Text
    - type: field_group
      name: link
      label: Link
      fields:
      - name: type
        type: select
        default: ''
        config:
          required: false
          options:
          - collectionLink
          - productLink
          - externalLink
          source:
            type: simple
            section: 
            file: 
            path: 
        label: Type
        description: Type of the link. If you want to link to a page, use `externalLink`.
      - type: text
        name: slug
        label: Slug
        description: If `collectionLink` or `productLink`, use the handle found in
          Shopify (e.g. "kids-outerwear"). If `externalLink`, this is used as-is,
          i.e. use "/pages/some-page/" for links to a particular page, or "https://www.google.com"
          for links that are truly external.
    description: Link button
  - type: text
    name: anchor
    label: Cropping anchor
    hidden: true
  label: Hero images
- name: sections
  type: blocks
  label: Content
  template_types:
  - contentSection
  - imageWithContent
  - imageBanner
  - shopByAge
  - testimonials-depr
  - blogPosts
  - collectionList
  config:
    min: 
    max: 
pages:
- demo/content/_index.md
