---
label: 'Deprecated: Shop by Age'
hide_body: true
fields:
- name: title
  type: text
  config:
    required: false
  label: Title
- name: items
  type: field_group_list
  fields:
  - name: image
    type: file
    config:
      maxSize: 64
    label: Image
  - name: text
    type: text
    config:
      required: false
    label: Text
  - type: field_group
    name: link
    label: Link
    fields:
    - name: type
      type: select
      default: ''
      config:
        required: false
        options:
        - page
        - collectionLink
        - productLink
        - externalLink
        source:
          type: simple
          section: 
          file: 
          path: 
      label: Type
    - type: text
      name: slug
      label: Slug
  config:
    min: 
    max: 
    labelField: 
  label: Links
