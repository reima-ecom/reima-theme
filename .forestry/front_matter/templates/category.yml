---
label: Category
hide_body: true
display_field: heading
fields:
- name: heading
  type: text
  config:
    required: true
  label: Heading
- name: collection
  type: text
  config:
    required: false
  label: Collection
- name: image
  type: file
  config:
    maxSize: 64
  label: Image
- name: categories
  type: blocks
  label: Sub-categories
  template_types:
  - category
  config:
    min: 
    max: 
